import { SignUp } from '@clerk/nextjs';

export const metadata = {
  title: 'Sign Up — E3 Digital Investor Readiness',
  description: 'Create your account',
};

const SignUpPage = () => <SignUp />;

export default SignUpPage;
